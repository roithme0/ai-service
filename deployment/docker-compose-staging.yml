name: ai-service-staging

services:
  backend:
    image: roithme0/ai-service-backend:alpha
    restart: unless-stopped

  frontend:
    image: roithme0/ai-service-frontend:alpha
    restart: unless-stopped
    depends_on:
      - backend

  gateway:
    image: nginx:1.25
    ports:
      - "${FRONTEND_PORT:-8001}:80"
    volumes:
      - ./gateway/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
      - frontend
